install.packages("devtools")
# Install "fueleconomy" dataset from GitHub
devtools::install_github("hadley/fueleconomy")
# Use the `libary()` function to load the "fueleconomy" package
library(fueleconomy)
# You should now have access to the `vehicles` data frame
# You can use `View()` to inspect it
View(vehicles)
# Select the different manufacturers (makes) of the cars in this data set.
# Save this vector in a variable
car_makes <- select(makes)
# Select the different manufacturers (makes) of the cars in this data set.
# Save this vector in a variable
makes <- vehicles$makes
# Select the different manufacturers (makes) of the cars in this data set.
# Save this vector in a variable
makes <- vehicles$make
view(make)
View(makes)
# Use the `unique()` function to determine how many different car manufacturers
# are represented by the data set
unique(makes)
View(uique(makes))
View(unique(makes))
# Filter the data set for vehicles manufactured in 1997
filter(vehicles$year == "1997")
# Filter the data set for vehicles manufactured in 1997
filter(vehicles$year == "1997",)
# Filter the data set for vehicles manufactured in 1997
filter(,vehicles$year == "1997")
# Filter the data set for vehicles manufactured in 1997
filter(vehicles$year == 1997)
# Use the `unique()` function to determine how many different car manufacturers
# are represented by the data set
length(unique(makes))
car1997
car_1997
source('~/Desktop/info/ch10-dplyr/exercise-1/exercise.R', echo=TRUE)
# Filter the data set for vehicles manufactured in 1997
car_1997 <- vehicles[vehicles$year == 1997,]
car_1997
view(car_1997)
View(car_1997)
# Arrange the 1997 cars by highway (`hwy`) gas milage
# Hint: use the `order()` function to get a vector of indices in order by value
# See also:
# https://www.r-bloggers.com/r-sorting-a-data-frame-by-the-contents-of-a-column/
car_1997 <- cars_1997[order(cars_1997$hwy), ]
# Arrange the 1997 cars by highway (`hwy`) gas milage
# Hint: use the `order()` function to get a vector of indices in order by value
# See also:
# https://www.r-bloggers.com/r-sorting-a-data-frame-by-the-contents-of-a-column/
car_1997 <- car_1997[order(cars_1997$hwy), ]
car_1997 <- car_1997[order(cars_1997$hwy), ]
# Install and load the "dplyr" library
install.packages("dplyr")
library(dplyr)
source('~/Desktop/info/ch10-dplyr/exercise-2/exercise.R', echo=TRUE)
makes
# Use the `distinct()` function to determine how many different car manufacturers
# are represented by the data set
distinct_makes <- distinct(vehicles, make)
View(vehicles)
# Use the `distinct()` function to determine how many different car manufacturers
# are represented by the data set
distinct_makes <- distinct(vehicles, make)
distinct_makes
# Filter the data set for vehicles manufactured in 1997
cars_1997 <- filter(year, year == 1997)
# Filter the data set for vehicles manufactured in 1997
cars_1997 <- filter(vehicles, year == 1997)
cars_1997
distinct(cars_1997, year)
# Filter the data set for vehicles manufactured in 1997
cars_1997 <- filter(vehicles, year == 1997)
# Arrange the 1997 cars by highway (`hwy`) gas milage
better_hwy_mpg <- arrange(vehivles, hwy)
# Arrange the 1997 cars by highway (`hwy`) gas milage
better_hwy_mpg <- arrange(vehivles, hwy)
better_hwy_mpg <- arrange(vehivles, hwy)
# Arrange the 1997 cars by highway (`hwy`) gas milage
better_hwy_mpg <- arrange(vehicles, hwy)
# Arrange the 1997 cars by highway (`hwy`) gas milage
better_hwy_mpg <- arrange(vehicles, hwy)
better_hwy_mpg
# Arrange the 1997 cars by highway (`hwy`) gas milage
better_hwy_mpg <- arrange(vehicles, -hwy)
better_hwy_mpg
